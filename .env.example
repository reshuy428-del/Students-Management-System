# Environment variables for PostgreSQL (example)
# Copy this file to `.env` or set these variables in your system before running the app.
PG_HOST=localhost
PG_PORT=5432
PG_DB=studentsdb
PG_USER=postgres
PG_PASSWORD=changeme
